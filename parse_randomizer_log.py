import argparse
import pathlib
import random
from parse_randomizer_htm_log import PkmnRandomizerHtmLogParser
from parse_randomizer_txt_log import PkmnRandomizerTextLogParser

# should probably declare some pokemon data types to keep me honest
# pokemon are just a typed dict so it shouldnt be bad


class PkmnRandomizerLogParser:
    def __init__(self) -> None:
        raise RuntimeError(
            "Construct this class using class method from_htm or from_log"
        )

    @classmethod
    def from_htm(cls, htm_logfile_path: str = ""):
        with open(htm_logfile_path, "r", encoding="windows-1252") as htmfp:
            parser = PkmnRandomizerHtmLogParser(htmfp)
            return parser

    @classmethod
    def from_txt(cls, logfile_path: str = ""):
        parser = PkmnRandomizerTextLogParser(logfile_path)
        return parser


def test():
    print("Test.")
    htm_parser:PkmnRandomizerHtmLogParser = PkmnRandomizerLogParser.from_htm("./testing/test_emerald.gba.log.html")
    txt_parser:PkmnRandomizerTextLogParser = PkmnRandomizerLogParser.from_txt("./testing/test_zx_emerald.gba.log")

    test_pkmn = "METAGROSS"
    htm_testpkmn = htm_parser.get_stats_for_pkmn(test_pkmn)
    txt_testpkmn = txt_parser.get_stats_for_pkmn(test_pkmn)

    print(htm_testpkmn)
    print(txt_testpkmn)

    htm_testpkmn_locs = htm_parser.get_locations_for_pkmn(test_pkmn)
    txt_testpkmn_locs = txt_parser.get_locations_for_pkmn(test_pkmn)

    print(list(htm_testpkmn_locs))
    print(list(txt_testpkmn_locs))

def create_args_parser():
    # old parser: 
    parser = argparse.ArgumentParser("parse_randomizer_log")
    parser.add_argument(
        "logfile",
        help="The logfile generated by the randomizer",
        type=pathlib.Path)
    
    parser.add_argument(
        "-o",
        "--outfile",
        help="File to write the parsed Pokemon data to",
        type=argparse.FileType('w', encoding="utf-8")
    )

    parser.add_argument("-f", "--find",
                        help="Pokemon to retrieve all possible locations for")

    parser.add_argument(
        "--noloc",
        help="When extracting Pokemon, do not include their locations.",
        action=argparse.BooleanOptionalAction)
    
    return parser

if __name__ == "__main__":
    # test()
    args_parser = create_args_parser()
    args = args_parser.parse_args()

    # if args.find:
    #     print(f"finding locations of {args.find}")
    #     res = find_pokemon_locations_in_file(args.logfile, args.find)
    # elif args.withloc:
    #     print(f"grabbing pokemon and their locations")
    #     (pkmn, locs) = extract_from_file(args.logfile, True)
    #     res = [f"{p}: {', '.join(locs[p])}" for p in pkmn]
    # else:
    #     print(f"grabbing pokemon")
    #     pkmn = extract_from_file(args.logfile, False)
    #     res = list(pkmn)

    # if args.outfile:
    #     write_wild_pkmn_to_file(args.outfile, res)
    # else:
    #     print(res)